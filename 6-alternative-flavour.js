// For the First Module continuation lecture, README.md time stamp at 47:15

module.exports.items = ["item1", "item2"]; // We are setting a property on that object: module.exports.items, and setting it equal to an array: ["item1", "item2"]

const person = {
  name: "bob",
};

module.exports.singlePerson = person; // We are setting a property on that object: module.exports.singlePerson, and setting it equal to a variable: person
